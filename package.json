{
  "name": "soundconnect-monorepo",
  "private": true,
  "version": "0.1.0",
  "engines": {
    "node": ">=20"
  },
  "scripts": {
    "build": "npm ci --prefix frontend && npm run build --prefix frontend",
    "start": "sh start.sh"
  }
}

